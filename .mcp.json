{
  "mcpServers": {
    "semaphor": {
      "command": "node",
      "args": [
        "scripts/semaphor-mcp-remote.mjs"
      ],
      "env": {
        "SEMAPHOR_PROJECT_TOKEN": "${SEMAPHOR_PROJECT_TOKEN}"
      }
    }
  }
}
